// const User = require('../../../../core-services/auth-service/src/models/User');

// const getDeviceTokensByPincode = async (pincode) => {
//   const users = await User.find({ pincode });
//   return users.map(user => user.deviceToken);
// };

// module.exports = { getDeviceTokensByPincode };